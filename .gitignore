# Secrets — jamais commités
.env
.env.*
!.env.example
*.key
*.pem
secrets/

# OS
.DS_Store
Thumbs.db
desktop.ini

# Editeurs
.vscode/settings.json
*.swp
*~

# Session context (fichiers de travail personnels — ne pas partager)
CONTEXT.md

# Temporaire
*.log
*.tmp

# Archives (repos uploadés, fichiers volumineux)
*.zip
*.tar.gz

# MCP dynamic config (peut contenir des clés API)
.mcp.json
