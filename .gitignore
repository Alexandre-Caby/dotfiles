# Secrets — never committed
.env
.env.*
!.env.example
*.key
*.pem
secrets/

# OS
.DS_Store
Thumbs.db
desktop.ini

# Editors
.vscode/settings.json
*.swp
*~

# Session context (personal working files — do not share)
CONTEXT.md

# Temporary
*.log
*.tmp

# Archives (uploaded repos, large files)
*.zip
*.tar.gz

# MCP dynamic config (may contain API keys)
.mcp.json

# Claude Code runtime artifacts (generated, never committed)
claude/.credentials.json
claude/backups/
claude/cache/
claude/debug/
claude/history.jsonl
claude/plugins/
claude/projects/
claude/shell-snapshots/
claude/todos/
claude/plans/
claude/claude
